# EEE 环境配置文件示例
# 复制此文件为 .env 并根据需要修改配置

# =============================================================================
# 代理配置 (Proxy Configuration)
# =============================================================================

# 启用/禁用代理 (true/false)
EEE_PROXY_ENABLED=false

# HTTP 代理地址
# HTTP_PROXY=http://proxy.example.com:8080

# HTTPS 代理地址
# HTTPS_PROXY=http://proxy.example.com:8080

# FTP 代理地址
# FTP_PROXY=http://proxy.example.com:8080

# 不使用代理的地址列表（逗号分隔）
# NO_PROXY=localhost,127.0.0.1,*.local,*.internal

# =============================================================================
# GitHub 配置 (GitHub Configuration)
# =============================================================================

# 首选的 GitHub 镜像源（按优先级排序，逗号分隔）
# 可选值: github, gitee, fastgit, ghproxy, jsdelivr, gitclone, cnpmjs
# 国内用户推荐: gitee,fastgit,github
EEE_GITHUB_MIRROR=gitee,fastgit,github

# GitHub 连接超时时间（秒）
EEE_GITHUB_TIMEOUT=15

# GitHub 操作重试次数
EEE_GITHUB_RETRY=3

# =============================================================================
# 日志配置 (Logging Configuration)
# =============================================================================

# 日志级别 (debug, info, warn, error)
EEE_LOG_LEVEL=info

# 日志文件路径（可选，不设置则只输出到控制台）
# EEE_LOG_FILE=/var/log/eee.log

# =============================================================================
# 安装配置 (Installation Configuration)
# =============================================================================

# 容器模式（在 Docker 容器中运行时启用）
EEE_CONTAINER_MODE=false

# 跳过 Docker 服务启动（在不需要 Docker 服务的环境中）
EEE_SKIP_DOCKER_SERVICE=false

# 默认 Shell
EEE_DEFAULT_SHELL=zsh

# =============================================================================
# 网络配置 (Network Configuration)
# =============================================================================

# 网络操作超时时间（秒）
EEE_NETWORK_TIMEOUT=30

# 网络操作重试次数
EEE_NETWORK_RETRY=3

# 用户代理字符串
EEE_USER_AGENT=EEE-Installer/1.0

# =============================================================================
# 高级配置 (Advanced Configuration)
# =============================================================================

# 强制使用特定的包管理器
# EEE_PACKAGE_MANAGER=apt

# 跳过系统更新
# EEE_SKIP_SYSTEM_UPDATE=false

# 启用详细输出
# EEE_VERBOSE=false

# 启用调试模式
# EEE_DEBUG=false

# =============================================================================
# 环境特定配置 (Environment Specific)
# =============================================================================

# 开发环境配置
# EEE_ENV=development

# 生产环境配置
# EEE_ENV=production

# CI/CD 环境配置
# EEE_ENV=ci

# =============================================================================
# 配置说明 (Configuration Notes)
# =============================================================================

# 1. 以 # 开头的行是注释，不会被解析
# 2. 配置格式为 KEY=VALUE，等号两边不要有空格
# 3. 值可以用单引号或双引号包围，也可以不用引号
# 4. 环境变量的优先级高于 .env 文件中的配置
# 5. 布尔值请使用 true/false
# 6. 数组值请使用逗号分隔
# 7. 路径建议使用绝对路径

# =============================================================================
# 常用配置组合 (Common Configuration Combinations)
# =============================================================================

# 国内用户推荐配置:
# EEE_GITHUB_MIRROR=gitee,fastgit,github
# EEE_GITHUB_TIMEOUT=15

# 企业环境配置:
# EEE_PROXY_ENABLED=true
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NO_PROXY=localhost,*.internal,*.company.com

# 开发环境配置:
# EEE_LOG_LEVEL=debug
# EEE_VERBOSE=true
# EEE_DEBUG=true

# 生产环境配置:
# EEE_LOG_LEVEL=warn
# EEE_LOG_FILE=/var/log/eee.log
# EEE_CONTAINER_MODE=true
